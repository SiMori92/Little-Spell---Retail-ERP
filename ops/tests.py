from django.test import TestCase

# Create your tests here.
# Slice A test-output rule: never print a customer-data row on failure.
# Assert on identifiers and counts, never on a rendered row. Public CI logs are readable.
